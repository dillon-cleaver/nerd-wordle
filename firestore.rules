rules_version = '2';

// WARNING: These rules allow full read/write access to anyone. 
// This is for development only. 
// TODO: Set to 'false' before deploying to production to secure your database.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
