/**
 * UI Opacity Constants
 *
 * Standardized opacity values for consistent visual hierarchy
 */

export const opacity = {
  /** Subtle text, secondary information, disabled states */
  subtle: 0.8,
  /** Moderate emphasis, hover states */
  moderate: 0.6,
  /** Low emphasis, placeholders, very subtle elements */
  low: 0.4,
  /** Pressed state for touchable elements */
  pressed: 0.7,
} as const;
